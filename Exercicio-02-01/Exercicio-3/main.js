let titulo = "Isto é um título longo de um livro";
console.log(titulo.length);

titulo = titulo.replace("livro", "diário");
console.log(titulo.replace("livro", "diário"));

let novoTitulo = titulo + " mas não faz mal porque não é uma seca";
console.log(novoTitulo);

console.log(titulo.substring(0, 4));

